# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Secrets and local analysis artifacts (never commit)
.env
.env.local
.env.*.local
supabase_artifacts/

# Virtual Environment
venv/
env/
ENV/
.venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Data
data/raw/
# Generated file
data/processed/driver_characteristics.json
data/validation/
*.parquet

# Replay outputs (hundreds of MB, regenerable, machine-local)
data/historical_replay/
# Sidecar replay roots from scripts/replay_historical_checkpoints.py --output-root.
# Each is ~19MB of regenerable measurement output; the bare pattern above missed them.
data/historical_replay_*/

# FastF1 Cache
.fastf1_cache/

# Testing & Coverage
.pytest_cache/
.coverage
coverage.xml
htmlcov/
*.cover
.hypothesis/

# Archive & Backups
# NOTE: archive/ contains dead code moved for reference (circuit.py, legacy predictors)
# Keep gitignore to prevent accidental commits, but files exist locally
archive/utils/
archive/predictors/
src_backup/
*.backup

# Notebooks (exploration)
notebooks/dirty_exploration/

# Temporary
*.tmp
*.log
changes.txt
*.ipynb_checkpoints/
archive/
.uv-cache/
PROGRESS_REPORT.md
FIXME.md
reports/
notebooks/

# Local Claude preview scaffolding (machine-specific)
.claude/launch.json
.claude/settings.local.json

# Generated artifacts and tool state (regenerable, machine-local)
# Snapshots are ArtifactStore output mirrored from Supabase in production; the
# tracked copies were a stale naming-migration leftover, not a required input.
data/car_characteristics_snapshot/
data/systems/
# PredictionLogger writes here at runtime and also saves to ArtifactStore. Tracked
# copies made the dashboard load each prediction twice -- once from the file, once
# from Supabase -- which PredictionLogger._deduplicate_predictions then collapsed
# with a warning. The three that were tracked are published to Supabase; archived
# to ~/Documents/trackside-labs-archive/predictions_local_20260806.tar.gz.
data/predictions/
.impeccable/
# `*.backup` above does NOT match `<name>.json.rebuild_backup`, hence this line.
*.rebuild_backup
